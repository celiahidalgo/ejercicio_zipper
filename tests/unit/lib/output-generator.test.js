const fs = require("fs");
const outputGenerator = require("../../../lib/output-generator");

describe("Testing de setListeners", () => {
    it("Debería devolver los bits que ocupa el archivo", () => {
        input = outputStream.on;
    })
})